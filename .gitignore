node_modules/
dist/
build/
.cache/
.env
.env.local
.env.*.local
frontend/.env
.vite/
vite.config.*.timestamp-*
*.tsbuildinfo
*-debug.log*
logs/
*.log
.claude-replay.*
.tmp-*
__pycache__/
*.pyc
_tmp_test.jpg
backend/**/__pycache__/

# Model weight files (large binary files — keep out of git)
backend/weights/*.pt
backend/weights/*.pkl
*.pt

# Database files
data/*.db

# Platform-managed agentplat state, not project source. In the
# client-initiated single-repo workspace layout (make's `Path: "."` spec) the
# repo root IS the workspace root, so these land inside this repo rather than
# beside it.
/.plugins/
/.opencode-skills/
